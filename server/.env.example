# 서버
RIBBON_HOST=0.0.0.0
RIBBON_PORT=8765

# 제공자 선택: mock 으로 두면 모델 없이 전체 흐름이 돌아감
RIBBON_STT_PROVIDER=mock            # mock | faster_whisper | mlx_whisper (맥미니)
# 대화 모델은 관리자 페이지 → 설정 → 🧠 대화 모델 에서 고른다. 아래 LLM 값은 처음 한 번 그 칸을 채우는 데만 쓴다
RIBBON_LLM_PROVIDER=mock            # mlx (맥미니 mlx-serve) | ollama | openai (기타 OpenAI 호환) | mock
RIBBON_TTS_PROVIDER=browser         # browser | mac_say | supertonic (권장, pip install supertonic)
RIBBON_TTS_VOICE=F1                 # supertonic 음성: M1~M5, F1~F5
RIBBON_TTS_SPEED=1.05               # 0.7 ~ 2.0
RIBBON_TTS_STEPS=8                  # 품질 5 ~ 12
RIBBON_WAKEWORD_PROVIDER=mock       # mock | energy (말소리 나는 채널이 자동으로 깨어남, 호출어 없이 테스트용) | openwakeword

# TV 배경 렌더 (블렌더). 블렌더가 없으면 TV 는 실시간 3D 화면
RIBBON_BLENDER_EXE=                 # 비우면 /Applications/Blender.app 등에서 찾음
RIBBON_RENDER_AUTO=true             # 맵 편집기 저장 뒤 자동 렌더
RIBBON_RENDER_PCT=50                # 50 = 1920x1080, 100 = 3840x2160
RIBBON_RENDER_SAMPLES=96

# LLM (Ollama 또는 mlx-lm 서버, OpenAI 호환 엔드포인트)
RIBBON_LLM_BASE_URL=http://localhost:11434/v1
RIBBON_LLM_MODEL=gemma3:27b         # qwen3 계열은 생각 텍스트가 섞여 나올 수 있음
RIBBON_LLM_API_KEY=ollama
RIBBON_LLM_MAX_TOKENS=400
RIBBON_LLM_NO_THINK=true            # qwen3 생각 모드 끄기 (답이 비면 이게 꺼져 있는지 확인)
RIBBON_LLM_AUTOLOAD=false           # mlx-serve 면 true (모델이 내려가 있으면 답하기 전에 올린다)

# STT
RIBBON_STT_MODEL=large-v3-turbo     # large-v3-turbo (빠름) | large-v3 (더 정확, 느림) | medium. 못 받으면 turbo 로 돌아감

# 호출어
RIBBON_WAKE_PHRASE=리본아
RIBBON_WAKEWORD_MODEL_PATH=
RIBBON_WAKEWORD_THRESHOLD=0.5

# 오디오
RIBBON_CHANNELS=4
RIBBON_SAMPLE_RATE=16000
RIBBON_VAD_SILENCE_MS=800
RIBBON_MAX_UTTERANCE_S=15
RIBBON_FOLLOW_UP_WINDOW_S=6
# 리본이가 말하는 동안 + 끝난 뒤 0.7초는 마이크를 듣지 않는다 (스피커 소리가 다시 질문이 되는 것 막기)
RIBBON_ECHO_GUARD=true
RIBBON_ECHO_TAIL_MS=700

# 대기열
RIBBON_WAITING_TIMEOUT_S=20
RIBBON_TURN_IDLE_TIMEOUT_S=10

# 데이터
RIBBON_KIDS_FILE=../data/kids.json
