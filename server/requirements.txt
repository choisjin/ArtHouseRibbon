# Core (all environments). ASCII only: pip on Windows reads this file with the system codepage.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
httpx>=0.27

# Tests
pytest>=8
pytest-asyncio>=0.23
